### `npm run dev`

Runs the app in development mode with Vite.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

The page will hot-reload as you make edits.

### `npm test`

Runs the test suite once with Vitest. Use `npm run test:watch` for interactive watch mode.

### `npm run build`

Type-checks the project and builds it for production to the `build` folder.\
The build is minified and the filenames include content hashes.

### `npm run preview`

Serves the production build locally, to sanity-check it before deploying.

# FILM ROULETTE APP

https://filmroulette.netlify.app/

This project was created using Typescript & Redux Toolkit.

## Redux

I used RTK Query to obtain information from MovieDB.

## Styling (Sass)

Style is minimalist and straightforward and has been created using the css framework Sass. I also added global variables so as not to repeat values.

## Routing

In order to generate all the routes, I have used the react-router-dom library in its latest version and a 404 page component has been added to avoid errors when typing.

## Componentization, reusability and customHooks

I have tried to componentize and divide the application into smaller pieces of code so there's no repetition. 

### Ana Gracia
